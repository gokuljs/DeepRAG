Rank the movies listed below by relevance to the following search query.

Query: "{query}"

Movies:
{doc_list_str}

Return ONLY a raw JSON array of the movie IDs in order of relevance (best match first). Do NOT wrap in markdown code fences, do NOT include any explanation — output the bare array only.

For example:
[75, 12, 34, 2, 1]
where the movie with idx 75 is most relevant.

Ranking:
